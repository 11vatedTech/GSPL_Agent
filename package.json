{
  "name": "gspl-agent",
  "version": "0.1.0",
  "private": true,
  "description": "GSPL AI Agent — the standalone, sovereign, GSPL-native intelligence system.",
  "type": "module",
  "engines": { "node": ">=20.0.0" },
  "workspaces": ["packages/*", "adapters/*"],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  },
  "license": "MIT",
  "author": "11vatedTech",
  "repository": { "type": "git", "url": "https://github.com/11vatedTech/GSPL_Agent" }
}
