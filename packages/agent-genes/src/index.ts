/**
 * @gspl/agent-genes
 *
 * GSPL Agent Cognitive Gene Types.
 *
 * Extends the canonical 17-gene inventory with five cognitive gene types
 * that enable sovereign agent cognition:
 *
 *   intent     — Teleological objective vector
 *   belief     — Probabilistic epistemic propositions
 *   memory     — Temporal-spatial memory graph with decay
 *   policy     — Enforceable invariant network (capability security)
 *   hypothesis — Speculative cognitive branch for simulation
 *
 * Each type has full operator semantics: mutation, crossover, distance,
 * validation, canonicalization, composition, merge, diff, IR lowering/lifting.
 */

export * from './types.js';
export * from './defaults.js';
export * from './registry.js';
