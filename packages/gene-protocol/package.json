{
  "name": "@gspl/gene-protocol",
  "version": "0.1.0",
  "description": "GSPL Gene Protocol — extensible, versioned gene type registry replacing the fixed 17-type inventory",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "scripts": { "build": "tsc", "typecheck": "tsc --noEmit", "test": "vitest run" },
  "license": "MIT",
  "engines": { "node": ">=20.0.0" },
  "dependencies": { "@gspl/canon-foundation": "*" },
  "devDependencies": { "vitest": "^2.0.0" }
}
